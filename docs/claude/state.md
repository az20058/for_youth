# 상태 관리 규칙

- **Props 과도한 드릴링 금지** — props를 **3단계 이상** 전달해야 하는 경우 계속 드릴링하지 마라.
- 그런 경우 `zustand` 같은 전역/공유 상태 솔루션을 사용한다.
- 스토어는 최소화하고 범위를 좁게 유지한다 — 모든 상태를 zustand로 옮기지 마라.
- 로컬 상태를 우선으로 하고, 컴포넌트 계층 구조로 props 관리가 복잡해질 때만 zustand로 올린다.
